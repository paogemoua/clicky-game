import React from "react";
import "/Jumbotron.css";

var Jumbotron = () => (
    <header className = "header">
        <h1>Clicky-Click-Click Game @ Bob's Burgers</h1>
        <h2>Click on an image to earn points, but don't click on any more than once! Don't forget!</h2>
    </header>
);

export default Jumbotron;